# webprobe
WebProbe is a microservices based e commerce platform that crawls permitted web sources, extracts product information, tracks price history, and exposes the collected data through REST APIs. It uses Spring Boot, RabbitMQ, PostgreSQL, and concurrent crawler workers to process and manage web data efficiently.
